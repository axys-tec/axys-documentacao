# Scripts com credenciais hardcoded — NÃO versionar enquanto não forem env-based.
# Quando as credenciais virarem variáveis de ambiente, remova estas linhas e versione.
gerar_backup_render.py
restaurar_local.py

__pycache__/

# Dumps do banco (DADOS) — nunca vão pro git.
*.dump
*.sql
*.sql.gz
*.backup
